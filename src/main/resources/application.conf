ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.mipt.ktbook.MainKt.ktbook ]
    }
}

jwt {
    secret = ${KTBOOK_SECRET}
    issuer = "http://0.0.0.0:8080/"
    audience = "http://0.0.0.0:8080/"
    realm = "Access to ktbook"
}
